{
  "name": "app-spectacle",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "build-to-prod": "rm -rf ../app-spectacle-package/* && meteor build --server-only ../app-spectacle-package && mv ../app-spectacle-package/*.tar.gz ../app-spectacle-package/package.tar.gz",
    "send-to-prod":"scp ../app-spectacle-package/package.tar.gz appspectacle@149.202.50.62:~/app-spectacle"
  },
  "dependencies": {
    "babel-runtime": "^6.18.0",
    "bcrypt": "^1.0.2",
    "meteor-node-stubs": "^0.2.6",
    "moment": "^2.18.1",
    "simpl-schema": "^0.2.3"
  }
}
